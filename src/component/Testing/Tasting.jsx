import React from "react";
import gsap from "gsap";
import { ScrollTrigger } from "gsap/all";

// Register ScrollTrigger (no need to register useGSAP)
gsap.registerPlugin(ScrollTrigger);

const Tasting = () => {
  

 

  return (
   <h1>Testing</h1>
  );
};

export default Tasting;
